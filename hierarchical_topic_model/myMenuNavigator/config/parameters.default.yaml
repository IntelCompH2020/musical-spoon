# Default parameters

# Parameters for the dataset selection
dataset:
  # Source file name
  path2source: 'C:\mallet\data_news_txt_500'

# Specify format for the log outputs
logformat:
  filename:    msgs.log
  datefmt:     '%m-%d %H:%M:%S'
  file_format: '%(asctime)s %(levelname)-8s %(message)s'
  file_level:  INFO
  cons_level:  DEBUG
  cons_format: '%(levelname)-8s %(message)s'
